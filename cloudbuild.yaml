# Use BUILD_ID for image tags: it is always defined (including `gcloud builds submit`
# from a local directory). COMMIT_SHA is only set for repo-triggered builds.
steps:
  - name: "gcr.io/cloud-builders/docker"
    args: ["build", "-t", "us-central1-docker.pkg.dev/$PROJECT_ID/proctoride/frontend:$BUILD_ID", "./frontend"]

  - name: "gcr.io/cloud-builders/docker"
    args: ["build", "-t", "us-central1-docker.pkg.dev/$PROJECT_ID/proctoride/backend:$BUILD_ID", "./backend"]

  - name: "gcr.io/cloud-builders/docker"
    args: ["push", "us-central1-docker.pkg.dev/$PROJECT_ID/proctoride/frontend:$BUILD_ID"]

  - name: "gcr.io/cloud-builders/docker"
    args: ["push", "us-central1-docker.pkg.dev/$PROJECT_ID/proctoride/backend:$BUILD_ID"]

  - name: "gcr.io/google.com/cloudsdktool/cloud-sdk"
    entrypoint: gcloud
    args:
      [
        "run",
        "deploy",
        "proctoride-backend",
        "--image",
        "us-central1-docker.pkg.dev/$PROJECT_ID/proctoride/backend:$BUILD_ID",
        "--region",
        "us-central1",
        "--platform",
        "managed",
        "--allow-unauthenticated",
      ]

  - name: "gcr.io/google.com/cloudsdktool/cloud-sdk"
    entrypoint: gcloud
    args:
      [
        "run",
        "deploy",
        "proctoride-frontend",
        "--image",
        "us-central1-docker.pkg.dev/$PROJECT_ID/proctoride/frontend:$BUILD_ID",
        "--region",
        "us-central1",
        "--platform",
        "managed",
        "--allow-unauthenticated",
      ]

# Required when Cloud Build trigger uses a custom service_account:
# logs must target Cloud Logging, a logs bucket, or REGIONAL_USER_OWNED_BUCKET behavior.
options:
  logging: CLOUD_LOGGING_ONLY
